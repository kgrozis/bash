#! /bin/bash

# here-documnent << chars
#   redirects text from the command line 

# view ext file
# paramter sent to ext is substituted in grep statement
bash ext bill
bash ext x.555

# could add -i to grep statement in ext to make it case sensitive.